"""
Template for a new tab's Help content — copy to ``<tab_slug>.py`` and fill in.

This file is NOT loaded (name starts with ``_``). Remove the leading
underscore from the copy's filename when you add a real tab.

Steps:
1. Copy this file to ``energy_dashboard/content/help/my_tab.py``
2. Set ``TAB_CLASS`` to your tab widget's class name (must match ``__name__``)
3. Write ``HELP_TEXT`` as HTML (same subset as About / changelog: h2, p, ul, li, b, i, code)
4. Restart not required — the package re-discovers modules on import

The global status-bar Help button looks up ``TAB_CLASS`` automatically.
"""
from __future__ import annotations

# TAB_CLASS = "MyNewTab"

# HELP_TEXT = """\
# <h2>My New Tab</h2>
# <p>One-paragraph overview of what this tab does.</p>
# <p><b>Section heading</b></p>
# <ul>
# <li>Bullet one.</li>
# <li>Bullet two.</li>
# </ul>
# """
